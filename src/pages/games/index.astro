---
import Layout from '../../layouts/Layout.astro'
import GameLongCard from '../../components/GameLongCard.astro'
import { allGames } from '../../data/games'


---

<Layout titleKey="site.title">
  <section class="pt-34 pb-16 max-w-4xl mx-auto">
    <div class="px-4 sm:px-6 lg:px-8">
      <!-- Games List -->
      <div class="flex flex-col gap-8">
        {allGames.map((game) => (
          <GameLongCard
            titleKey={game.titleKey || ''}
            descriptionKey={game.descriptionKey || ''}
            devDetailKey={game.devDetailKey}
            image={game.imgCover}
            href={`/games/${game.id}`}
            platforms={game.platforms || []}
            leftMemoTextKey={game.leftMemoTextKey}
            leftMemoColor={game.leftMemoColor}
            rightMemoTextKey={game.rightMemoTextKey}
            rightMemoColor={game.rightMemoColor}
            r18={game.r18}
            haveDetailPage={game.haveDetailPage}
          />
        ))}
      </div>
    </div>
  </section>
</Layout>

<script>
  import { updateContent } from '../../scripts/i18n'
  updateContent()
</script>
